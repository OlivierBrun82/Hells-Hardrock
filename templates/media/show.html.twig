{% extends 'base.html.twig' %}

{% block title %}Media{% endblock %}

{% block body %}
    <h1>Media</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medium.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ medium.Title }}</td>
            </tr>
            <tr>
                <th>GroupName</th>
                <td>{{ medium.GroupName }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ medium.Style }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ medium.Year }}</td>
            </tr>
            <tr>
                <th>Medium</th>
                <td>{{ medium.Medium }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_media_index') }}">back to list</a>

    <a href="{{ path('app_media_edit', {'id': medium.id}) }}">edit</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
