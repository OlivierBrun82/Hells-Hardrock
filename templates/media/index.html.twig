{% extends 'base.html.twig' %}

{% block title %}Hells Hardrock - Collection{% endblock %}

{% block body %}
<div class="container">
    <h1 class="page-title">🎸 Collection Hells Hardrock</h1>
    
    <div class="table-responsive">
        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Groupe</th>
                    <th>Style</th>
                    <th>Année</th>
                    <th>Support</th>
                    <th>Propriétaire</th>
                    <th>Emprunteur</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for medium in media %}
                <tr>
                    <td>{{ medium.title }}</td>
                    <td>{{ medium.groupName }}</td>
                    <td>{{ medium.style }}</td>
                    <td>{{ medium.year }}</td>
                    <td>{{ medium.medium }}</td>
                    <td>{{ medium.owner ? medium.owner.firstName ~ ' ' ~ medium.owner.lastName : 'N/A' }}</td>
                    <td>{{ medium.borrower ? medium.borrower.firstName ~ ' ' ~ medium.borrower.lastName : 'Disponible' }}</td>
                    <td>
                        <a href="{{ path('app_media_show', {'id': medium.id}) }}" class="btn btn-sm btn-outline-primary">Voir</a>
                        <a href="{{ path('app_media_edit', {'id': medium.id}) }}" class="btn btn-sm btn-outline-warning">Modifier</a>
                        {{ include('media/_delete_form.html.twig') }}
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="8" class="text-center">Aucun média trouvé</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    
    <div class="text-center mt-4">
        <a href="{{ path('app_media_new') }}" class="btn btn-primary btn-lg">🎵 Ajouter un nouveau média</a>
    </div>
</div>
{% endblock %}