<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}MetalVault{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_media_index') }}">🎸 MetalVault</a>
                <div class="navbar-nav">
                    <a class="nav-link" href="{{ path('app_media_index') }}">Collection</a>
                    <a class="nav-link" href="{{ path('app_media_new') }}">Ajouter</a>
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    {% endif %}
                </div>
            </div>
        </nav>

        <main class="main-content">
            <div class="container">
                {% block body %}{% endblock %}
            </div>
        </main>

        <footer class="footer">
            <div class="container">
                <p>&copy; 2024 MetalVault - Votre collection Rock & Métal</p>
            </div>
        </footer>
    </body>
</html>