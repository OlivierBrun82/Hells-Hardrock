<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}MetalVault - Collection Musicale{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🎸</text></svg>">
        {% block stylesheets %}
            {{ importmap('app') }}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <!-- Navigation -->
        <nav class="navbar">
            <div class="container">
                <a href="{{ path('app_home') }}" class="navbar-brand">🎸 METALVAULT</a>
                <ul class="navbar-nav">
                    <li><a href="{{ path('app_home') }}" class="nav-link">Collection</a></li>
                    <li><a href="{{ path('app_media_new') }}" class="nav-link">Ajouter un média</a></li>
                </ul>
            </div>
        </nav>

        <!-- Contenu principal -->
        <main class="container">
            <div class="main-content">
                {% block body %}{% endblock %}
            </div>
        </main>

        <!-- Footer -->
        <footer class="footer">
            <div class="container">
                <p>&copy; 2024 MetalVault -  Musicale Rock & Métal</p>
            </div>
        </footer>
    </body>
</html>
